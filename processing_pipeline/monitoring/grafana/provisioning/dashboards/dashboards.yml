apiVersion: 1

providers:
  - name:            OTT Pipeline
    orgId:           1
    folder:          OTT
    type:            file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
